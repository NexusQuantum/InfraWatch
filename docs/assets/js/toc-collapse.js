// Minimal no-op placeholder so Lotus Docs picks up the custom JS asset.
// Keeps parity with the reference repo config (custom_js entry).
(function () {
  // Reserved for future TOC collapse behavior.
})();
